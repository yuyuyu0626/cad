# HccePose — inference-oriented Python dependencies (pins aligned with README.md / README_CN.md).
#
# Install PyTorch from the official CUDA wheel index FIRST, e.g.:
#   pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu128
#
# Then:
#   pip install -r requirements-inference.txt
#
# Notes:
# - Intended for Python 3.10 (reference interpreter in the README).
# - Does NOT include bpy (Blender Python) or blenderproc — only needed for synthesis/training workflows.
# - RGB-D FoundationPose path still needs diffrp-nvdiffrast (included below).
# - ONNX / onnxruntime-gpu may also be installed on first Tester(..., hccepose_acceleration='onnx') if missing.
# - Optional (same as README): python -c "import imageio; imageio.plugins.freeimage.download()"

ultralytics==8.3.233
fvcore==0.1.5.post20221221
pybind11==2.12.0
trimesh==4.2.2
ninja==1.11.1.1
kornia==0.6.9
open3d==0.19.0
transformations==2024.6.1
numpy==1.26.4
opencv-python==4.9.0.80
opencv-contrib-python==4.9.0.80
scipy==1.15.3
kiwisolver==1.4.9
matplotlib==3.10.7
imageio==2.37.2
pypng
Cython==3.2.1
PyOpenGL
triangle
glumpy
Pillow==11.3.0
vispy
imgaug
mathutils
pyrender
pytz
tqdm
tensorboard
kasal-6d==0.1.3
rich==14.2.0
h5py==3.15.1
diffrp-nvdiffrast
huggingface_hub[hf_transfer]==0.36.0
